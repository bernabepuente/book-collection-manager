export const environment = {
  production: true,
  // In a real deployment this would be injected at build time via CI (e.g. --define)
  // or baked in through environment variables in the CI pipeline
  apiUrl: 'https://your-api-domain.com',
};
