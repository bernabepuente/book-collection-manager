name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend – test & build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Run tests
        working-directory: backend
        run: mvn verify -q

      - name: Build Docker image
        working-directory: backend
        run: docker build -t book-collection-backend:${{ github.sha }} .

  frontend:
    name: Frontend – test & build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Run unit tests
        working-directory: frontend
        run: npm test -- --watch=false --browsers=ChromeHeadless

      - name: Build production bundle
        working-directory: frontend
        run: npm run build -- --configuration production

  # ---------------------------------------------------------------------------
  # DEPLOY – Azure App Service (concept, runs on main only)
  #
  # Prerequisites (configured as GitHub Actions secrets):
  #   AZURE_CREDENTIALS   – Service principal JSON from:
  #                         az ad sp create-for-rbac --sdk-auth
  #   REGISTRY_LOGIN_SERVER  – e.g. myregistry.azurecr.io
  #   REGISTRY_USERNAME      – ACR admin username
  #   REGISTRY_PASSWORD      – ACR admin password
  #   AZURE_WEBAPP_NAME      – Name of the Azure App Service web app
  #
  # Steps:
  #   1. az login with the service principal.
  #   2. docker build + push to Azure Container Registry (ACR).
  #   3. az webapp config container set to point App Service to the new image.
  #   4. App Service pulls the image and restarts automatically.
  #
  # Uncomment and fill in the resource names to activate.
  # ---------------------------------------------------------------------------
  # deploy:
  #   name: Deploy to Azure App Service
  #   runs-on: ubuntu-latest
  #   needs: [backend, frontend]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Log in to Azure
  #       uses: azure/login@v1
  #       with:
  #         creds: ${{ secrets.AZURE_CREDENTIALS }}
  #
  #     - name: Log in to Azure Container Registry
  #       uses: azure/docker-login@v1
  #       with:
  #         login-server: ${{ secrets.REGISTRY_LOGIN_SERVER }}
  #         username: ${{ secrets.REGISTRY_USERNAME }}
  #         password: ${{ secrets.REGISTRY_PASSWORD }}
  #
  #     - name: Build and push Docker image to ACR
  #       working-directory: backend
  #       run: |
  #         IMAGE=${{ secrets.REGISTRY_LOGIN_SERVER }}/book-collection-backend:${{ github.sha }}
  #         docker build -t $IMAGE .
  #         docker push $IMAGE
  #
  #     - name: Deploy to Azure App Service
  #       uses: azure/webapps-deploy@v2
  #       with:
  #         app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
  #         images: ${{ secrets.REGISTRY_LOGIN_SERVER }}/book-collection-backend:${{ github.sha }}
